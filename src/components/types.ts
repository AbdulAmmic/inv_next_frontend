interface Product {
  id: string;
  name: string;
  category: string;
  sku: string;
  stockQuantity: number;
  unit: string;
  costPrice: number;
  sellingPrice: number;
  profitMargin: number;
  unitsSold: number;
  variants: number;
  store: string;
}